%h1
  = @activity.name
  %small= @activity.suite.name

= render partial: "navigation", locals: { activity: @activity, active: :destroy }

= closed_suite_message(@activity.suite)

= confirm_destroy_form(@activity, t(:".confirmation.#{@activity.type}"))
