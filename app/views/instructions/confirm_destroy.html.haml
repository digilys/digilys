%h1
  = @instruction.title
  %small= Instruction.model_name.human

= render partial: "navigation", locals: { active: :edit }

= confirm_destroy_form(@instruction, t(:".confirmation"), cancel_to: params[:return_to] || root_url())
