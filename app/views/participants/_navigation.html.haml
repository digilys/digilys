- active     ||= nil
- suite      ||= nil

- content_for :title do
  - Participant.model_name.human(count: 2)

%ul.nav.nav-tabs
  - if suite
    %li{class: active_if(active == :new)}
      %a{href:new_suite_participant_path(suite)}= t(:".new_action")
    %li
      %a{href:suite_path(suite)}= t(:".return_to_suite")
